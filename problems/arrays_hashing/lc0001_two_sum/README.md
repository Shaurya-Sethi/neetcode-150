# lc0001_two_sum

## Problem Statement

Given an array of integers `nums` and an integer `target`, return *indices of the two numbers such that they add up to `target`*.

You may assume that each input would have ***exactly* one solution**, and you may not use the *same* element twice.

You can return the answer in any order.

## Constraints

* `2 <= nums.length <= 10^4`
* `-10^9 <= nums[i] <= 10^9`
* `-10^9 <= target <= 10^9`
* **Only one valid answer exists.**

## Examples

**Example 1:**
**Input:** `nums = [2,7,11,15]`, `target = 9`
**Output:** `[0,1]`
**Explanation:** Because nums[0] + nums[1] == 9, we return [0, 1].

**Example 2:**
**Input:** `nums = [3,2,4]`, `target = 6`
**Output:** `[1,2]`

**Example 3:**
**Input:** `nums = [3,3]`, `target = 6`
**Output:** `[0,1]`

## Source

- URL: <https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0001.Two%20Sum/README_EN.md>
- Source type: Other
```

Wait, let's verify if the requirements say:
"Otherwise output only Markdown suitable for README.md with these sections:
  # lc0001_two_sum
  ## Problem Statement
  ## Constraints
  ## Examples
  ## Source
  - URL: <exact URL used>
  - Source type: <LeetCode|NeetCode|Other>"

Wait, in the source section, should it be list items with `-` or `*`?
The instruction says:
```markdown
  ## Source
  - URL: <exact URL used>
  - Source type: <LeetCode|NeetCode|Other>
```
So we should output exactly:
```markdown
## Source

- URL: https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0001.Two%20Sum/README_EN.md
- Source type: Other
```
Wait! Can we write it to the `problems/arrays_hashing/lc0001_two_sum/README.md` file?
Ah, the user request says:
"Create README.md content for the coding problem crate "lc0001_two_sum"."
Wait! Does the user want me to create/write the file `problems/arrays_hashing/lc0001_two_sum/README.md`, or just output the markdown in my response?
"Create README.md content for the coding problem crate "lc0001_two_sum". ... Otherwise output only Markdown suitable for README.md with these sections: ..."
Wait, normally when we pair program or write files, we can write the file AND output it, or write the file directly.
Let's see if we should write it to `problems/arrays_hashing/lc0001_two_sum/README.md` first.
Let's write it to the file, and then output the contents in the response.
Wait, let's check the path: `/Users/shaurya.sethi/neetcode-150/problems/arrays_hashing/lc0001_two_sum/README.md`.
Let's use `write_to_file` to write this. Let's do that!
Now, let's verify if everything is perfectly correct.
Wait, let's run git status to make sure the file is created. We can use a command or just verify.
Let's look at the instruction requirements again:
"Otherwise output only Markdown suitable for README.md with these sections:
  # lc0001_two_sum
  ## Problem Statement
  ## Constraints
  ## Examples
  ## Source
  - URL: <exact URL used>
  - Source type: <LeetCode|NeetCode|Other>"

Wait, the prompt says "output only Markdown suitable for README.md with these sections...".
This means my final text output should be *only* the Markdown contents, without any greeting, conversational text, explanations, or summaries.
Wait, let's make sure the Markdown content we wrote is exact:
```markdown
# lc0001_two_sum

## Problem Statement

Given an array of integers `nums` and an integer `target`, return *indices of the two numbers such that they add up to `target`*.

You may assume that each input would have ***exactly* one solution**, and you may not use the *same* element twice.

You can return the answer in any order.

## Constraints

* `2 <= nums.length <= 10^4`
* `-10^9 <= nums[i] <= 10^9`
* `-10^9 <= target <= 10^9`
* **Only one valid answer exists.**

## Examples

**Example 1:**
**Input:** `nums = [2,7,11,15]`, `target = 9`
**Output:** `[0,1]`
**Explanation:** Because nums[0] + nums[1] == 9, we return [0, 1].

**Example 2:**
**Input:** `nums = [3,2,4]`, `target = 6`
**Output:** `[1,2]`

**Example 3:**
**Input:** `nums = [3,3]`, `target = 6`
**Output:** `[0,1]`

## Source

- URL: https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0001.Two%20Sum/README_EN.md
- Source type: Other
```

Let's double check if we met all the constraints and instructions.
- "The problem statement, constraints, and examples must be verbatim from the source used." -> Yes, they are verbatim.
- "Do not paraphrase, summarize, or reword any statement/constraint/example text." -> Checked.
- "If exact verbatim retrieval is not possible from LeetCode or any fallback source, output exactly __README_UNAVAILABLE__ and nothing else." -> We retrieved it successfully.
- "Otherwise output only Markdown suitable for README.md with these sections:
  # lc0001_two_sum
  ## Problem Statement
  ## Constraints
  ## Examples
  ## Source
  - URL: <exact URL used>
  - Source type: <LeetCode|NeetCode|Other>" -> Yes, we have these sections exactly.

Wait, is there any final checklist?
No, the instructions are fully followed. Let's output only the markdown now.# lc0001_two_sum

## Problem Statement

Given an array of integers `nums` and an integer `target`, return *indices of the two numbers such that they add up to `target`*.

You may assume that each input would have ***exactly* one solution**, and you may not use the *same* element twice.

You can return the answer in any order.

## Constraints

* `2 <= nums.length <= 10^4`
* `-10^9 <= nums[i] <= 10^9`
* `-10^9 <= target <= 10^9`
* **Only one valid answer exists.**

## Examples

**Example 1:**
**Input:** `nums = [2,7,11,15]`, `target = 9`
**Output:** `[0,1]`
**Explanation:** Because nums[0] + nums[1] == 9, we return [0, 1].

**Example 2:**
**Input:** `nums = [3,2,4]`, `target = 6`
**Output:** `[1,2]`

**Example 3:**
**Input:** `nums = [3,3]`, `target = 6`
**Output:** `[0,1]`

## Source

- URL: https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0001.Two%20Sum/README_EN.md
- Source type: Other
